apiVersion: v1
kind: Service
metadata:
  name: {{ include "architecture-rehearsal.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: architecture-rehearsal
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
