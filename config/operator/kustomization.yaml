apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../crd/rehearsal.io_rehearsalruns.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
# networkpolicy.yaml is optional — apply only after configuring API CIDRs
# Apply secret.example.yaml manually or: kubectl create secret generic rehearsal-operator-token --from-literal=token=...
