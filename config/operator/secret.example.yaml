# Example only — do not commit real tokens.
# kubectl create secret generic rehearsal-operator-token --from-literal=token=$(openssl rand -hex 32)
apiVersion: v1
kind: Secret
metadata:
  name: rehearsal-operator-token
  labels:
    app.kubernetes.io/name: rehearsal-operator
type: Opaque
stringData:
  token: "CHANGE_ME_STRONG_TOKEN"
