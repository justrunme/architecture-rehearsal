{
  "apiVersion": "rehearsal.io/v1alpha1",
  "kind": "prometheus-rule",
  "id": "change-prom-rule-cluster-label",
  "title": "Add PrometheusRule with cluster=acme-prod selector on kube_pod_status_ready",
  "description": "cluster label exists on 'up' but not on kube_pod_status_ready — zero series.",
  "seeds": ["alert/KubePodNotReady"],
  "facts": {
    "scenario": "prom-zero-match",
    "metric": "kube_pod_status_ready",
    "expr": "kube_pod_status_ready{condition=\"false\",cluster=\"acme-prod\"} > 0",
    "label_matchers": {
      "cluster": "acme-prod"
    }
  }
}
